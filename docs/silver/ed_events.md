| Column Name            | Data Type     | Description                    | Source                                                                      | Transformations                                                                                |
| ---------------------- | ------------- | ------------------------------ | --------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| event_occurance_id     | BIGINT        | Surrogate key / auto-increment | Identity column                                                             | Auto-generated                                                                                 |
| event_id               | INT           | Unique event identifier        | `bronze.ed_events.event_id`                                                 | Trim, remove quotes, convert empty string to NULL                                              |
| event_name             | NVARCHAR(255) | Name of the event              | `bronze.ed_events.event_name`                                               | Trim, remove quotes, convert empty string to NULL                                              |
| start_datetime         | DATETIME2(0)  | Event start timestamp          | `bronze.ed_events.start_datetime`                                           | Trim, remove quotes, TRY_CONVERT to DATETIME2                                                  |
| start_date_key         | BIGINT        | Surrogate key for start date   | `silver.create_date_key(start_datetime)`                                    | Generate via date key function                                                                 |
| end_datetime           | DATETIME2(0)  | Event end timestamp            | `bronze.ed_events.end_datetime`                                             | Trim, remove quotes, TRY_CONVERT to DATETIME2                                                  |
| end_date_key           | BIGINT        | Surrogate key for end date     | `silver.create_date_key(end_datetime)`                                      | Generate via date key function                                                                 |
| agency_id              | TINYINT       | Event agency reference         | `bronze.ed_events.event_agency`                                             | Trim, remove quotes, map via `silver.ed_event_agency` lookup                                   |
| event_type_id          | TINYINT       | Event type reference           | `bronze.ed_events.event_type`                                               | Trim, remove quotes, map via `silver.ed_event_type` lookup                                     |
| borough_id             | TINYINT       | Borough where event occurs     | `bronze.ed_events.event_bourough`                                           | Trim, remove quotes, map via `silver.borough` lookup                                           |
| street_closure_type_id | TINYINT       | Street closure type reference  | `bronze.ed_events.street_closure_type`                                      | Trim, remove quotes, replace empty with 'none', map via `silver.ed_street_closure_type` lookup |
| reject_id              | TINYINT       | Record validation flag         | Derived from `silver.validate_events_records(start_datetime, end_datetime)` | Validation procedure; marks invalid records                                                    |
